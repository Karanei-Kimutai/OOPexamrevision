package abstractclassesandinterfaces;

public interface interfaces {
    //Interfaces are structures used to create abstract methods
    //Interfaces have no constructors
    //Interfaces are used to provide a common functionality to a group of unrelated classes
    //All attributes in an interface are implicitly public,static and final(constants)
    //All methods are implicitly public and abstract
    //Interfaces are meant to be implemented by classes and their abstract methods are meant to be overriden

    //Attributes
    double salaryconstant= 0.7;


    //methods
    double compute();
}
